# Everything the Python code actually imports.
requests>=2.31.0        # every fetcher
python-dotenv>=1.0.0    # .env loading

# Only needed if you generate natural-language explanations
# (i.e. run demo_pipeline.py without --no-explanations).
anthropic>=0.39.0
